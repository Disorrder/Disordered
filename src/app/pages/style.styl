$root = "../../"

#app
    background url($root + "assets/images/bg_main.png")
    background-size cover
    background-position bottom center

.layout
    .content
        min-height calc(100vh - 24px)
